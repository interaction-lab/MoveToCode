namespace MoveToCode {
    public class CodeBlockEnums {
        public enum Move {
            Forward,
            Backward
        }
        public enum Turn {
            Right,
            Left
        }
    }
}